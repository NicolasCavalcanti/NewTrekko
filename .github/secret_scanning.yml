# The Supabase anon key is intentionally public — it is the browser-side
# client key designed to be shipped with frontend bundles. GitHub's secret
# scanner flags it as a secret, but it carries no elevated privileges.
# Exclude compiled/minified asset files from secret scanning.
paths-ignore:
  - "assets/**"
  - "dist/**"
